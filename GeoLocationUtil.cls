public class GeoLocationUtil {
    public static Decimal calculateDistance(Decimal loc1, Decimal loc2) {
        return Math.abs(loc1 - loc2); // Simplified for demo; replace with Haversine formula if needed
    }
}
